import React from "react";

class Broadcast extends React.Component {
    constructor(props) {
        super(props);
        // props.client.join(convId);
    }

    render() {
        return <p>Broadcast</p>;
    }
}

export default Broadcast;